Phase 2 React scaffold for SARA

Commands:

Install dependencies:

```bash
cd frontend/react-app
npm install
```

Run dev server:

```bash
npm run dev
```

Set API base url via `.env` as `VITE_API_BASE_URL` (default: http://127.0.0.1:5000)
